<div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
    </button>

</div>
<div class="navbar-collapse collapse">
    <ul class="nav navbar-nav">
        @if (User.Identity.IsAuthenticated)
        {
            <li>
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"><span class="glyphicon glyphicon-chevron-down"></span></a>
                <ul class="dropdown-menu" role="menu">
                    <li>@Html.ActionLink("Доска объявлений", "Index", "Board")</li>
                    <li>@Html.ActionLink("Новости компании", "Index", "New")</li>
                    <li class="divider"></li>
                    <li>@Html.ActionLink("Мои сообщения", "Index", "Dialog")</li>
                    <li>@Html.ActionLink("Мои объявления", "My", "Board")</li>
                    @if (User.IsInRole("Admin"))
                    {
                        <li class="divider"></li>
                        <li>@Html.ActionLink("Типы диалогов", "Index", "DialogType")</li>
                    }
                    <li class="divider"></li>
                    <li>@Html.ActionLink("Настройки профиля", "Index", "Manage")</li>
                    @if (User.IsInRole("Admin"))
                    {
                        <li>@Html.ActionLink("Настройка администрации", "DetailsAdmins", "Account")</li>
                    }
                </ul>
            </li>
        }
        <li>@Html.ActionLink("Главная", "Index", "Home")</li>
        <li>@Html.ActionLink("Контакты", "Contact", "Home")</li>
    </ul>
    @Html.Partial("_LoginPartial")
</div>